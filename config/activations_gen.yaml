test: False # if true, runs only on the first two dataset points

defaults:
  - _self_
  - dataset: conj1
  - model: Gemma4B
  - task: bin_class
  - probe: null
  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled

hydra:  
  output_subdir: null
  run:
    dir: .
